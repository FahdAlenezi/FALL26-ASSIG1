// Portal code: Incident Management. This is the VICTIM in Mission 2.
// You do NOT edit this file. You attack it from the DevTools Console.

document.getElementById("purge-btn").addEventListener("click", () => {
  document.getElementById("log").textContent =
    "PURGE REQUESTED at " + new Date().toLocaleTimeString();
});
